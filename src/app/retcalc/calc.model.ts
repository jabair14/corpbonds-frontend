export interface Calc {
    yrlyIncome: number,
    currAge: number,
    retAge: number,
    monthlyCont: number,
    rateReturn: number,
    savBalance: number,
    retAccVal?: number,
    userId: number,
    id: number
}